# .env  – values only, *no* quotes or spaces around =
POSTGRES_USER=expense_user
POSTGRES_PASSWORD=expense_pass
POSTGRES_DB=expense_db

REDIS_PASSWORD=redis_pass

# OpenSSL-generated or any strong key (keep identical in backend .env)
JWT_PRIVATE_KEY=super-secret-jwt-key
